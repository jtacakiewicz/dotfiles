[opener]
edit = [
    { run = 'nvim "$@"', block = true, desc = "Neovim" }
]
xdg-open = [
    { run = 'xdg-open "$1"', desc = "Open with system default" }
]

[open]
prepend_rules = [
    { name = "*.txt", use = "edit" },
    { mime = "text/*", use = "edit" },
    { mime = "application/json", use = "edit" },
    { mime = "*", use = "xdg-open" },
    { name = "*/", use = [] } # Prevents directories from opening in external apps
]

